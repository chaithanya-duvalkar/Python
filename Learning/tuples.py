#A built-in data type that lets us create immutable sequence of values
#as lists are mutable but tuples as string they are immutable
#tup=()

###########
# TUPLE METHODS
# tup.index(val)
# tup.count(val)


tuples=(10,20,30,40)
print(tuples)
print(tuples[0])
# tuples[0]=40   tuples are immutable as string
# print(tuples)